# 隐私与数据处理

## 仓库承诺

本公开仓库不得包含：

- 真实用户名、昵称、账号 ID、头像地址或联系方式；
- Cookie、访问令牌、会话头、密码、短信码或配对码；
- 设备序列号、无线调试地址、精确位置或可关联个人的设备标识；
- 未经授权的原始评论全集、客服工单、内部报告或截图；
- 本机绝对路径、个人姓名、个人邮箱或内部组织信息。

仓库中的示例数据必须明确标注为合成数据。真实采集结果只保存在使用者自己的工作区，并由使用者决定保存期限和访问权限。

## 对外分享前的最低处理

1. 删除用户名、账号 ID 和头像 URL；需要关联上下文时使用随机或稳定伪名。
2. 删除 Cookie、令牌、密码、配对码、设备地址和序列号。
3. 检查评论正文是否主动披露电话、邮箱、地址、订单号、姓名或其他身份线索。
4. 对地区、设备、时间等组合字段做再识别风险评估；必要时泛化为区域、设备系列和周粒度。
5. 只分享完成任务所需的最少字段和最少样本。
6. 在报告中写明数据来源、匿名化方式、样本偏差和覆盖缺口。

## 平台与账号边界

- 不自动登录，不绕过访问控制。
- 不读取或导出浏览器 Cookie。
- 不发帖、评论、点赞、关注或改变账号状态。
- 不把查询驱动的社媒样本描述为所有用户的代表性结论。

## 提交前检查

提交公开仓库前至少扫描以下内容：

```powershell
rg -n -i --hidden "cookie|token|secret|password|passwd|device.?id|serial|C:\\Users\\|@[A-Za-z0-9._-]+" .
```

扫描结果需要人工复核：文档中的安全提醒会命中关键词，但凭据值、个人路径和真实标识不得存在。二进制文档和图片需要另行检查元数据与可见内容。

如果敏感信息已经进入 Git 历史，仅删除当前文件并不够；应在继续公开发布前重写历史并轮换所有可能泄露的凭据。
